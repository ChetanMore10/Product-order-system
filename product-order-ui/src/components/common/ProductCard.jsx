import { Card, CardContent, Chip, IconButton, Stack, Typography, Button, Box } from '@mui/material'
import { Add, Remove, ShoppingBagOutlined } from '@mui/icons-material'

export default function ProductCard({ product, quantity = 1, onQuantity, onAdd }) {
  return <Card className="soft-panel" sx={{ height: '100%', display: 'flex', flexDirection: 'column' }}><Box sx={{ height: 150, background: product.color || '#e8f2ed', display: 'grid', placeItems: 'center' }}><ShoppingBagOutlined sx={{ fontSize: 52, color: 'rgba(23,34,31,.28)' }} /></Box><CardContent sx={{ p: 2.25, flexGrow: 1 }}><Stack direction="row" justifyContent="space-between" alignItems="flex-start" gap={2}><Typography fontWeight={800}>{product.name}</Typography><Typography fontWeight={800} sx={{ whiteSpace: 'nowrap' }}>${product.price}</Typography></Stack><Typography variant="body2" color="text.secondary" sx={{ mt: .8, minHeight: 42 }}>{product.description}</Typography><Stack direction="row" justifyContent="space-between" alignItems="center" sx={{ mt: 2 }}><Chip label={product.active ? product.category : 'Unavailable'} size="small" sx={{ bgcolor: product.active ? '#e8f2ed' : '#f1eeee', color: product.active ? '#235c4b' : '#9b5d58', fontWeight: 700 }} />{product.active && <Stack direction="row" alignItems="center"><IconButton size="small" onClick={() => onQuantity?.(Math.max(1, quantity - 1))}><Remove fontSize="small" /></IconButton><Typography fontWeight={700}>{quantity}</Typography><IconButton size="small" onClick={() => onQuantity?.(quantity + 1)}><Add fontSize="small" /></IconButton></Stack>}</Stack>{onAdd && <Button fullWidth variant="outlined" disabled={!product.active} onClick={() => onAdd(product)} sx={{ mt: 2 }}>Add to cart</Button>}</CardContent></Card>
}
