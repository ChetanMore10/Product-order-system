import { useState } from 'react'
import { Alert, Box, Button, Link, Paper, Stack, TextField, Typography } from '@mui/material'
import { useNavigate } from 'react-router-dom'
import { useAuth } from '../../context/AuthContext'
export default function Login() { const { login } = useAuth(); const navigate = useNavigate(); const [form, setForm] = useState({ username: '', email: '', password: '' }); const [error, setError] = useState(''); const submit = async e => { e.preventDefault(); setError(''); try { await login(form) } catch (err) { setError(err.response?.data?.message || 'Login failed. Check your credentials and backend connection.') } }; return <Box sx={{ minHeight: '100vh', display: 'grid', placeItems: 'center', p: 2, bgcolor: '#f7f8f5' }}><Paper elevation={0} sx={{ width: '100%', maxWidth: 440, p: { xs: 3, sm: 5 }, border: '1px solid #e2e8e2', borderRadius: 4 }}><Stack direction="row" alignItems="center" gap={1.5} sx={{ mb: 4 }}><span className="brand-mark">PO</span><Typography fontWeight={800} sx={{ fontFamily: 'Manrope', fontSize: 20 }}>Parcelly</Typography></Stack><Typography variant="h4" className="page-title">Welcome back</Typography><Typography color="text.secondary" sx={{ mt: 1, mb: 3 }}>Sign in to your commerce workspace.</Typography>{error && <Alert severity="error" sx={{ mb: 2 }}>{error}</Alert>}<form onSubmit={submit}><Stack gap={2}><TextField label="Username" value={form.username} onChange={e => setForm({ ...form, username: e.target.value })} required /><TextField label="Email address" type="email" value={form.email} onChange={e => setForm({ ...form, email: e.target.value })} required /><TextField label="Password" type="password" value={form.password} onChange={e => setForm({ ...form, password: e.target.value })} required /><Button type="submit" variant="contained" size="large">Sign in</Button></Stack></form><Typography textAlign="center" color="text.secondary" sx={{ mt: 3 }}>New here? <Link component="button" onClick={() => navigate('/register')}>Create an account</Link></Typography></Paper></Box> }
