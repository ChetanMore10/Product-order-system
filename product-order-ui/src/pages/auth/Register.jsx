import { useState } from 'react'
import { Alert, Box, Button, Link, Paper, Stack, TextField, Typography } from '@mui/material'
import { useNavigate } from 'react-router-dom'
import { register } from '../../api/authApi'
export default function Register() { const navigate = useNavigate(); const [form, setForm] = useState({ username: '', email: '', password: '' }); const [error, setError] = useState(''); const [done, setDone] = useState(false); const submit = async e => { e.preventDefault(); setError(''); try { await register(form); setDone(true) } catch (err) { setError(err.response?.data?.message || 'Registration failed.') } }; return <Box sx={{ minHeight: '100vh', display: 'grid', placeItems: 'center', p: 2, bgcolor: '#f7f8f5' }}><Paper elevation={0} sx={{ width: '100%', maxWidth: 440, p: { xs: 3, sm: 5 }, border: '1px solid #e2e8e2', borderRadius: 4 }}><Typography variant="h4" className="page-title">Create an account</Typography><Typography color="text.secondary" sx={{ mt: 1, mb: 3 }}>Your account will be created by the backend.</Typography>{error && <Alert severity="error" sx={{ mb: 2 }}>{error}</Alert>}{done ? <Alert severity="success">Account created. <Link component="button" onClick={() => navigate('/login')}>Sign in</Link></Alert> : <form onSubmit={submit}><Stack gap={2}><TextField label="Username" value={form.username} onChange={e => setForm({ ...form, username: e.target.value })} required /><TextField label="Email address" type="email" value={form.email} onChange={e => setForm({ ...form, email: e.target.value })} required /><TextField label="Password" type="password" value={form.password} onChange={e => setForm({ ...form, password: e.target.value })} required /><Button type="submit" variant="contained" size="large">Create account</Button></Stack></form>}<Typography textAlign="center" color="text.secondary" sx={{ mt: 3 }}>Already registered? <Link component="button" onClick={() => navigate('/login')}>Sign in</Link></Typography></Paper></Box> }
