import { useState } from 'react'
import { Alert, Box, Button, Link, Paper, Stack, TextField, Typography } from '@mui/material'
import { useNavigate } from 'react-router-dom'
import { register } from '../../api/authApi'
export default function Register() { const navigate = useNavigate(); const [form, setForm] = useState({ username: '', email: '', password: '' }); const [error, setError] = useState(''); const [done, setDone] = useState(false); const [loading, setLoading] = useState(false); const submit = async e => { e.preventDefault(); setError(''); setLoading(true); try { await register(form); setDone(true) } catch (err) { setError(err.response?.data?.message || 'Registration failed.') } finally { setLoading(false); } }; return <Box sx={{ minHeight: '100vh', display: 'grid', placeItems: 'center', p: 2, bgcolor: '#f7f8f5' }}><Paper elevation={0} sx={{ width: '100%', maxWidth: 440, p: { xs: 3, sm: 5 }, border: '1px solid #e2e8e2', borderRadius: 2 }}><Stack direction="row" alignItems="center" gap={1.5} sx={{ mb: 4 }}><Box sx={{ width: 40, height: 40, bgcolor: '#1b5e3f', borderRadius: 1, display: 'grid', placeItems: 'center', color: '#fff', fontWeight: 800, fontSize: 16 }}>PO</Box><Typography fontWeight={800} sx={{ fontFamily: 'Manrope', fontSize: 20, color: '#17221f' }}>Parcelly</Typography></Stack><Typography variant="h5" sx={{ fontWeight: 700, color: '#17221f', mb: 1 }}>Create an account</Typography><Typography color="text.secondary" sx={{ mb: 3, fontSize: '.95rem' }}>Join to start shopping and managing your orders</Typography>{error && <Alert severity="error" sx={{ mb: 2, borderRadius: 1 }}>{error}</Alert>}{done ? <Alert severity="success" sx={{ borderRadius: 1 }}>Account created successfully! <Link component="button" onClick={() => navigate('/login')} underline="hover" sx={{ color: '#1b5e3f', fontWeight: 600 }}>Sign in now</Link></Alert> : <form onSubmit={submit}><Stack gap={2.5}><TextField label="Username" value={form.username} onChange={e => setForm({ ...form, username: e.target.value })} required fullWidth size="small" disabled={loading} /><TextField label="Email address" type="email" value={form.email} onChange={e => setForm({ ...form, email: e.target.value })} required fullWidth size="small" disabled={loading} /><TextField label="Password" type="password" value={form.password} onChange={e => setForm({ ...form, password: e.target.value })} required fullWidth size="small" disabled={loading} /><Button type="submit" variant="contained" size="large" disabled={loading} sx={{ mt: 1 }}>{loading ? 'Creating account...' : 'Create account'}</Button></Stack></form>}<Typography textAlign="center" color="text.secondary" sx={{ mt: 3, fontSize: '.9rem' }}>Already have an account? <Link component="button" onClick={() => navigate('/login')} underline="hover" sx={{ color: '#1b5e3f', fontWeight: 600 }}>Sign in</Link></Typography></Paper></Box> }
